# Frontend Environment Variables
# Set to your deployed backend URL in Vercel (e.g. https://delta-backend.onrender.com)
# When omitted in local development, it defaults to the Vite /api proxy
VITE_BACKEND_URL=
